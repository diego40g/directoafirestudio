rules_version = '2'; // Es buena práctica especificar la versión de las reglas

service cloud.firestore {
  match /databases/{database}/documents {
    match /tasks/{taskId} {
      allow read, write: if request.auth != null;
    }
  }
}